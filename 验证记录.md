# 首版本机流程验证

验证日期：2026-09-06。全部业务测试使用 TEST 虚构客户，未发送给任何客户。

| 检查 | 结果 |
|---|---|
| 硅基流动认证与模型读取 | 成功，文字 DeepSeek-V3.2，图片 Qwen3-VL-30B-A3B-Instruct |
| 文字询价 | 自动生成内部 Excel，报价 Q-20260906-C2131A |
| 图片询价 | 识别两个数量档，报价 Q-20260906-C6AD46；无印刷按来源自动归一 |
| Excel 询价 | 上传并读取单元格，生成报价 Q-20260906-F10B1E，两个数量档 |
| 本机审核入口 | 点击「审核／修改」确实在 WPS 打开指定文件 |
| WPS 实际改价 | C19 改为 0.025678 并保存；对外 H14 正确读取该值 |
| 修改后出单 | QS-20260906-C2131A-V2；50,000 pcs，USD 0.025678/pcs，显示总额 USD 1,283.90 |
| 多数量档出单 | 图片询价生成两份独立报价：50,000 pcs × USD 0.016244；100,000 pcs × USD 0.013070 |
| 原文件保护 | 生成前后内部 Excel 哈希一致；对外文件仅一张 Commercial Quotation，无内部成本页 |
| 图片/内部页/对外页渲染 | 已检查生成预览并核对值、单位、布局与公式错误 |
| 搜索与汇总 | 编号搜索返回对应一行；今日／近7天清单统计正确 |
| 页面运行 | 桌面与390px窄窗口可操作；没有页面脚本警告或错误 |
| 凭据 | 仅服务端私有文件保存，权限600；页面响应不包含密钥 |
| 访问控制 | 缺CSRF、外站Origin、请求私有文件均被拒绝 |

10 项专项测试全部通过：WPS改价、尺寸变更重算并保留售价、缺联系人阻止出单、两处售价冲突、数量档独立运费、缺尺寸不猜填、WPS空字符串兼容、报价编号匹配、图片无印刷归一、目录边界与防覆盖。

原有批量报价技能自身的核算自检全部通过。文字提取关闭额外思考模式后，单条虚构询价实测42秒；耗时受模型服务和资料规模影响。

WPS 会去掉中文表名两侧的引号，且空共享字符串在工作簿读取库中曾被误读。现已通过独立只读解析恢复原始单元格，并规范化内存中的公式引用；不改写源文件。

出现问题时生成的旧测试对外文件已移至 .data/verification/failed-output，可恢复；业务报价池保留验证正确的版本。

首版范围：单台电脑、现有平口袋/背心袋规则、每次最多50个规格数量行。尚未开展多人共享、长期运行、所有WPS/Excel版本和大规模真实询价验收。
