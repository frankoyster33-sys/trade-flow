# trade flow · 本机工作版

日常双击桌面的「trade flow」应用图标，自动启动并打开本机工作台。也可以双击同目录的「启动 trade flow.command」。项目文件夹移动后，重新运行「安装桌面入口.command」更新桌面入口。

1. 在「概览」上传 Excel、图片，或粘贴文字。可以同时上传多个附件。
2. 系统识别尺寸、数量、印刷和包装信息，使用已有批量报价规则生成内部 Excel。资料不足时，只补充提示的缺项。
3. 到「内部报价池」点击「审核／修改」，用本机 WPS 或 Excel 审核。价格和成本在原报价页修改；批量汇总也可改售价。「报价资料」保存客户联系人、印刷信息、币种和报价类型。「识别来源」保留字段对应的原资料。
4. 保存并关闭 Excel，回到系统点击「生成」。对外报价单使用实际保存的单价。

尺寸、数量、材质、印刷或核算参数改变时，系统会另存一个重新核算的内部版本；人工确认该版本后，再生成对外报价单。人工填定的售价会保留，并需要结合新成本重新审核。

多个数量档默认单独核算运费、分别生成报价单。客户明确同批出货且有来源依据，或你在 Excel 中把「出货组」设成同一组时，才按同批出货处理。不要把互斥的数量档放进同一出货组。

内部池：当前用户桌面的 `agent报价池/`

对外池：当前用户桌面的 `agent 对外报价单池/`

文件按日期保存，修改核算条件或再次出单会保留旧版本。列表支持客户、编号、规格搜索和日期筛选。概览提供今日／近 7 天报价清单。

模型设置使用硅基流动。密钥仅保存在本机服务端的私有配置文件；网页只显示是否已配置。导入的询价内容会通过该接口进行识别。可在设置中更换文字和图片模型，也可填写自己的系统设计署名。

当前已接入的核价规则支持 HDPE、LDPE、PP 的平口袋和背心袋。超出规则范围的情况仍需人工核价；印刷覆盖率、铜版块数等缺项不会猜填。未指定的原料价、汇率、包装和加价系数采用现有技能默认值，需在内部 Excel 审核是否适用。

当前是单台电脑使用的首版；多人同时审核、跨电脑共享和权限分工留待后续扩展。工作表名称和公式请保留；如需固定价格，可用确认后的数字替换单价。系统会提示冲突或不支持的改动。

文件夹里直接放入的其他 Excel 可以打开查看；要使用一键生成，请从本系统导入询价建立对应报价记录。

带 TEST 的客户和文件均是流程验证资料，请勿对外发送。
